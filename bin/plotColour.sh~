#!/bin/sh
#
find . -name "*.xml" -exec grep "<extent>" {} \; | cut -d ">" -f 2 | tr " " "\n" | tr "[:punct:]" " " | grep -E -i "b |col|sepia" | sed -e 's/^[ \t]*//;s/[ \t]*$//' |  sort | uniq -c > postcardColour.dat

